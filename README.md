# ai-site-builder

This project creates a website that generates websites using AI.

## Setup

1. Clone repo
2. Install dependencies:
   pip install -r requirements.txt

3. Add API key:
   export OPENAI_API_KEY=your_key_here

4. Run:
   python app.py

5. Open browser:
   http://127.0.0.1:5000
